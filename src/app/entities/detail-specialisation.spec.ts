import { DetailSpecialisation } from './detail-specialisation';

describe('DetailSpecialisation', () => {
  it('should create an instance', () => {
    expect(new DetailSpecialisation()).toBeTruthy();
  });
});
